{
  "name": "carasta",
  "private": true,
  "version": "0.0.1",
  "description": "Carasta — automotive auction & community platform",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "web": "pnpm --filter=@carasta/web dev"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
